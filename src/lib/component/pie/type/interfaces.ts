export interface PieChartConfig {
  radius: number;
  sidePadding: number;
  chartPadding: number;
  height: number;
  showLabels: boolean;
  labelTextColor: string;
  strokeWidth: number;
  strokeColor: string;
}
