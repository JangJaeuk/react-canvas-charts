export interface RadarChartConfig {
  radius: number;
  sidePadding: number;
  height: number;
  axisColor: string;
  gridColor: string;
  labelTextColor: string;
  gridTextColor: string;
  lineWidth: number;
  pointRadius: number;
  showGrid: boolean;
  showAxes: boolean;
  gridLevels?: number;
  maxValue?: number;
}

