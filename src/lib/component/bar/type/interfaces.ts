export interface BarChartConfig {
  barWidth: number;
  sidePadding: number;
  chartPadding: number;
  height: number;
  axisColor: string;
  gridColor: string;
  labelTextColor: string;
  gridTextColor: string;
  barSpacing: number;
}
